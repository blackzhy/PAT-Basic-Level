#include<iostream>
#include<vector>
using namespace std;

int main()
{
	int studentNum = 0, questionNum = 0;
	cin >> studentNum >> questionNum;
	auto questionCredit = new int[questionNum];
	vector<char> questionRight;
	for (int i = 0; i < questionNum; i++)
	{
		cin >> questionCredit[i];

	system("pause");
	return 0;
}